module.exports = (arg)=>{
	arg = (typeof arg).toUpperCase();
	switch (typeof arg) { // custom
		// most of the types are the same as javascript's, so we don't need anything here yet
	}

	return arg;
}