const readline  = require("readline");
const Interface = require("../../interface");

module.exports = class IO extends Interface {
    constructor(...args) {
        super(...args);

        // coming soon
    }
}